1. If the user inputs "a b ISBN !notAnInt! Author Title" it breaks the adding of the book. It adds that information but, it does not add ones after properly.
    a. Figure out if this happens with other wrong Book variables.
    b. Look in Book.cpp and BookList.cpp for an answer. (Already started this.)
    )